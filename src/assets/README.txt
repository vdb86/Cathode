Drop the tray icon here as tray.ico (preferred on Windows) or tray.png.
Recommended: 256x256 .ico containing 16/24/32/48 px frames.
CouchTube loads src/assets/tray.ico first, then tray.png; if neither exists the
tray still appears with a blank icon (app never fails to start over a missing file).
